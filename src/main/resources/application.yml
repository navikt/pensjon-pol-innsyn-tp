management:
  endpoint.prometheus.enabled: true
  endpoints.web.exposure.include: prometheus, health
spring.security.oauth2.client.registration.isso:
  authorization-grant-type: authorization_code
  client-id: nais.io/azure/AZURE_APP_CLIENT_ID
  client-name: ISSO
  client-secret: nais.io/azure/AZURE_APP_CLIENT_SECRET
  provider: isso
  scope: openid